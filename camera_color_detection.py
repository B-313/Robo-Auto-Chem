import cv2\nimport pandas as pd\nimport time\n\ndef color_detection_recorder(vial_number, camera_index=0, width=1280, height=720, fps=30, record_seconds=10):\n    # Initialize the VideoCapture object and set parameters\n    cap = cv2.VideoCapture(camera_index)\n    cap.set(cv2.CAP_PROP_FRAME_WIDTH, width)\n    cap.set(cv2.CAP_PROP_FRAME_HEIGHT, height)\n    cap.set(cv2.CAP_PROP_FPS, fps)\n\n    # Set the output file names\n    video_path = f'/home/robot/group_B/robo_chem_504/group_B_videos/{{vial_number + 1}}.mp4'\n    csv_path = '/home/robot/group_B/robo_chem_504/group_B_csv/color_detection.csv'\n\n    # Create a list to store logs\n    log_data = []\n\n    # Get the start time\n    start_time = time.time()\n    frame_count = 0\n\n    while (time.time() - start_time) < record_seconds:\n        ret, frame = cap.read()\n        if not ret:\n            break\n\n        # Process frame for color detection\n        hsv_frame = cv2.cvtColor(frame, cv2.COLOR_BGR2HSV)\n        # Define color ranges\n        red_mask = cv2.inRange(hsv_frame, (0, 70, 50), (10, 255, 255)) | cv2.inRange(hsv_frame, (170, 70, 50), (180, 255, 255))\n        yellow_mask = cv2.inRange(hsv_frame, (20, 100, 100), (30, 255, 255))\n        green_mask = cv2.inRange(hsv_frame, (40, 40, 40), (90, 255, 255))\n\n        color_detected = None\n        hue_value = None\n        if cv2.countNonZero(red_mask) > 0:\n            color_detected = 'RED'\n            hue_value = 0\n        elif cv2.countNonZero(yellow_mask) > 0:\n            color_detected = 'YELLOW'\n            hue_value = 30\n        elif cv2.countNonZero(green_mask) > 0:\n            color_detected = 'GREEN'\n            hue_value = 60\n\n        # Log the data\n        current_time = time.time() - start_time\n        log_data.append([vial_number, current_time, color_detected, hue_value, frame_count])\n        frame_count += 1\n\n    # Release the VideoCapture object and save log to CSV\n    cap.release()\n    df = pd.DataFrame(log_data, columns=['vial_number', 'timestamp', 'color_detected', 'hue_value', 'frame_count'])\n    df.to_csv(csv_path, index=False)\n    cv2.VideoWriter(video_path, cv2.VideoWriter_fourcc(*'mp4v'), fps, (width, height)).release()\n\n    print(f'Recording complete. Video saved to {video_path} and log saved to {csv_path}.')\n